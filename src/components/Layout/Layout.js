import React from 'react';
import Navigation from "../Navigation/Navigation"
const layout = () => {
    return (
        <React.Fragment>
            <Navigation/>
        </React.Fragment>
    );
};

export default layout;